General
  1. Securiser l'application


Watchers
  2. ajouter/deleter un Site/Selector
  3. scheduler l'enregistrement d'une version de snippet
  4. Visualiser les diff par date
  6. scheduler l'envoi d'un email si il y a une difference
  
  
  a. un job qui enregistre une fois par jour les snippets pojr chaque site (fetch_snippets.rake)
  b. un job qui calcul les difference et genere une image (s3) (detect_changes)
  c. un job qui envoi un email (avec image s3) si une difference a ete detectee
  
People
  1. form (firstname, lastname), affichage des title et company depuis linkedin

Filters
  Afficher des infos d'un site base sur les class et ids css3
  
  
todo:

- deploy sur heroku
- comment scheduler des jobs sur heroku
- scheduler fetch_snippets 5h
- modifier detect_changes pour sauver les images sur s3
- creer une table differences (date, snippet_id, ref vers image s3)
- modifier detect_changes pour remplir la table differences
- scheduler detect_changes 6h
- creer tache send_alerts qui envoi un email si une difference a ete detectee

