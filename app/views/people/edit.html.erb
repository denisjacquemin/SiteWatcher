<ul class="breadcrumb">
  <li><%= link_to 'Listing People', people_path %><span class="separator"> / </span></li>
  <li>Editing Person</li>
</ul>
<div class="sidebar">
  <div class="notice">
    <h4>Description</h4>
    <p>Person profiles.</p>
    
    <h4>Linkedin Profiles</h4>
    <p>The list of linkedin profiles matching the <i>firstname lastname</i></p>
    <p>Please select the valid profile, use the <i>Title, Region and Industry</i> columns to choose the right profile. When available you can also visualize the <i>past</i> positions when hovering on <i>(past)</i>.</p>
  </div>
</div>
<div class="content">
  <div class="block">
    <h1>
        <%= @person.firstname %> <%= @person.lastname %> <%= link_to "Next Person", next_person_path(@person), :class => 'small' %>
        <span class="right"><%= link_to 'Cancel', people_path, :class => 'btn' %></span>
    </h1>
    <%= render 'form' %>
  </div>
</div>
