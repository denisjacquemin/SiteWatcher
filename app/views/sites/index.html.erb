<ul class="breadcrumb">
  <li>Dashboard</li>
</ul>
<div class="sidebar">
  <div class="notice">
    <h4>Description</h4>
    <p>Follow updates on web pages</p>
    <h4>Watchers</h4>
    <p>See a Watcher like a robot who will crawl a web page every day. Each time an update is done on the page you receive an email with a screenshot of the page </p>
  </div>
</div>
<div class="content">
  <div class="block">
    <h1>
      Listing sites
      <span class="right"><%= link_to 'New Site', new_site_path, :class => 'btn primary' %></span>
    </h1>
    <table class="zebra-striped">
      <thead>
        <tr>
          <th>Name</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @sites.each do |site| %>
          <tr>
            <td><%= link_to site.name, site, :rel => "twipsy", :title => "#{site.url} / #{site.selector}" %></td>
            <td><span class="label important">rss</span>&nbsp;
            <% unless site.email.nil? %>
              <span class="label notice" rel="twipsy" data-original-title="Alert sent to <%= site.email %>">alert</span></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
